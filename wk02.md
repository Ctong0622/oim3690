# Week 2 Logs

### Session 03 - Things I Don't Understand

On Tuesday, we learned about plain HTML without CSS. While working on my personal homepage, I noticed several things in the code that I don't fully understand yet.

1. I don't understand what `<meta name="viewport" content="width=device-width, initial-scale=1.0">` does. I know it is part of the `<head>`, but I am not sure how it affects the webpage.

2. I see `<title>Ctong0622 | Personal Homepage</title>`, and I want to understand where this title actually appears and how it is different from the `<h1>` on the page.

3. I understand that `<h1>Ctong0622</h1>` displays my name as a heading, but I want to know what the difference is between `<h1>`, `<h2>`, and the other heading levels.

4. I know how to use an image URL in `<img src="">`, but I don't understand how to add my own image from my computer and make sure it still works after I push the website to GitHub.

5. I want to understand more about how all of these HTML tags work together and how the browser knows how to turn the HTML file into the webpage that I see.

##summary for session 3
Class 3 in One Paragraph

English: Class 3 introduced the essentials of plain HTML. HTML provides the structure and content of a webpage through elements such as headings, paragraphs, lists, images, and links. An HTML document separates information about the page in the <head> from visible content in the <body>, while attributes such as href and src provide additional information to elements. You also continued using Markdown for course notes and practiced giving Copilot structured prompts with clear goals, requirements, and constraints. The important workflow is not simply letting AI generate a webpage: prompt → AI generates → review and understand → test → commit → push.